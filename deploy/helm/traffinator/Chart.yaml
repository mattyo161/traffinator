apiVersion: v2
name: traffinator
description: Traffinator — predictive commute-time analyzer (Django API + React SPA + PostgreSQL)
type: application
# Baseline used for manual / chart-only publishes. On an app release, the chart
# is published with the release version (see .github/workflows/release.yml ->
# publish-chart), so chart and app track together.
version: 0.3.0
appVersion: "0.3.0"
keywords:
  - commute
  - traffic
  - django
  - react
home: https://github.com/mattyo161/traffinator
sources:
  - https://github.com/mattyo161/traffinator
maintainers:
  - name: mattyo161
